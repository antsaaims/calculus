---
title: Input and Output
description: A function's input goes in, gets processed, and an output comes out — one output for every input.
tags:
  - functions
---

A function is a machine: an **input** goes in, one **output** comes out.

$$
x \; (\text{input}) \;\longrightarrow\; f \;\longrightarrow\; f(x) \; (\text{output})
$$

Every input gets exactly one output — that's what makes it a function and not just any
old relationship between numbers.

**Math example:** for $f(x) = 2x + 1$, the input $x = 4$ produces the output
$f(4) = 9$.

**Real-world example:** a tip calculator takes the bill total as input and returns the
tip amount as output — one bill, one tip, every time.

**Further reading:** [Review of Functions](https://math.libretexts.org/Bookshelves/Calculus/Calculus_(OpenStax)/01:_Functions_and_Graphs/1.01:_Review_of_Functions) (LibreTexts / OpenStax Calculus)
