---
title: Sign of a Function
description: A function's value can be positive, negative, or zero at any given input.
tags:
  - functions
---

At any input, a function's output is either **positive** ($> 0$), **negative**
($< 0$), or exactly **zero**. That's its sign at that point.

See [[Reading Sign from a Graph]] for how to spot the sign just by looking at where the
graph sits relative to the $x$-axis.

**Math example:** for $f(x) = x - 3$: $f(5) = 2 > 0$, $f(1) = -2 < 0$, and
$f(3) = 0$.

**Real-world example:** a monthly profit function is positive when the business makes
money, negative when it loses money, and exactly zero at break-even.

**Further reading:** [Graphs of Functions](https://math.libretexts.org/Bookshelves/Precalculus/Precalculus_(Stitz-Zeager)/01:_Relations_and_Functions/1.6:_Graphs_of_Functions) (LibreTexts)
